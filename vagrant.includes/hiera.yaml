:hierarchy:
        - "nodes/%{clientcert}"
        - "nodes/%{hostname}"
        - "roles/%{role}"
        - "%{osfamily}"
        - localdev
        - common
:backends:
        - yaml
:yaml:
        :datadir: hieradata
